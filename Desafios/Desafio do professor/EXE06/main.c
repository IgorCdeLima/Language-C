#include <stdio.h>
#include <stdlib.h>

int main()
{
    int p, u, r, n, cont, quant;

    printf("TErmos de uma P.A\n");
    printf("\nPrimeiro termo: "); scanf("%d", &p);
    printf("\nUltimo termo: "); scanf("%d", &u);
    printf("\nRazao: "); scanf("%d", &r);

    for (p == p; p == u; p++)
    {
        p = p + r;
        printf("%d", p);
    }
    return 0;
}
